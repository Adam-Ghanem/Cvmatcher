import { CvWorkspace } from "@/components/app/CvWorkspace";

export const metadata = {
  title: "Your CV workspace | CVMatcher",
};

export default function WorkspacePage() {
  return <CvWorkspace />;
}
